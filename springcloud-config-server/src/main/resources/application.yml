eureka:
  client:
    service-url:
      defaultZone:  http://localhost:1001/eureka/

server:
  port: 1003

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri:


